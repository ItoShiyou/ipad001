<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>ja</string>
	<key>CFBundleDisplayName</key>
	<string>ScoreStand</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<true/>
	</dict>

	<!-- 楽譜PDFをユーザー自身がFilesアプリから出し入れできるようにする。
	     書き出したライブラリの取り回しもこれで完結し、サーバーを持たずに済む。 -->
	<key>UIFileSharingEnabled</key>
	<true/>
	<key>LSSupportsOpeningDocumentsInPlace</key>
	<true/>

	<!-- iPadを譜面台として使う以上、向きは奏者が決める。全方向を許可する。 -->
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>

	<!-- Split View / Slide Over / Stage Manager に対応する（FR-16）。
	     楽譜の横に音源や動画を並べて練習するのが実際の使われ方であるため。 -->
	<key>UIRequiresFullScreen</key>
	<false/>

	<!-- 書き出したライブラリを、このアプリの書類として扱えるようにする（FR-60）。 -->
	<key>UTExportedTypeDeclarations</key>
	<array>
		<dict>
			<key>UTTypeIdentifier</key>
			<string>com.example.ScoreStand.library</string>
			<key>UTTypeDescription</key>
			<string>ScoreStand ライブラリ</string>
			<key>UTTypeConformsTo</key>
			<array>
				<string>public.data</string>
			</array>
			<key>UTTypeTagSpecification</key>
			<dict>
				<key>public.filename-extension</key>
				<array>
					<string>scorestand</string>
				</array>
			</dict>
		</dict>
	</array>

	<!-- このアプリはネットワークを一切使わない（NFR-07）。
	     したがってネットワーク関連の用途説明キーを一つも持たない。 -->

	<!-- チューナー（FR-51、ベータ）専用。音声はその場でピッチ解析するだけで、
	     保存も送信もしない（送信する経路自体が存在しない。NFR-07参照）。 -->
	<key>NSMicrophoneUsageDescription</key>
	<string>チューナー機能で、演奏音の高さをその場で解析するために使います。録音や保存、外部への送信は一切行いません。</string>
</dict>
</plist>
